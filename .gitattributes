# Set default behaviour, case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be normalized and converted
# to native line endings on checkout.
*.cfg text
*.txt text
*.conf text
*.properties text
*.xml text
*.lang text
*.recipes text
*.lang text
*.prop text
*.json text
*.md text
